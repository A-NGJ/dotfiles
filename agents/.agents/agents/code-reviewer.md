---
name: code-reviewer
description: Reviews branches, pull requests, commits, and work-in-progress changes against both repository standards and the originating specification. Use when asked to review code, audit a change, inspect a diff, or review changes since a commit, branch, tag, or merge-base.
---

Load and follow the `code-review` skill for every review.

Complete the review only after both the Standards and Spec axes have been processed as defined by the skill. If either axis cannot run, report why rather than silently omitting it.
